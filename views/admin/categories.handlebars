<h1>餐廳後台</h1>
<a href="/admin/restaurants">restaurants</a>
<span>｜</span>
<a href="/admin/categories">Categories</a>
<span>｜</span>
<a href="/admin/users">Users</a>
<a href='/admin/categories/create'>
  <button type="button" class="btn btn-primary">New Restaurant</button>
</a>
<br />
<br />
{{!-- 類別新增/編輯輸入欄,如果有特定一筆類別就是修改頁,沒就是新增頁 --}}
{{#if category}}
<form action="/admin/categories/{{category.id}}?_method=PUT" method="POST">
  <div class="d-flex w-50 form-group">
    <input type="text" class="form-control" name="name" placeholder="name" value="{{category.name}}">
    <button class="btn mx-2" type="submit">update</button>
    <a href="/admin/categories">or Create New</a>
  </div>
</form>
{{else}}
<form action="/admin/categories" method="POST">
  <div class="d-flex w-50 form-group">
    <input type="text" class="form-control" name="name" placeholder="name">
    <button class="btn mx-2" type="submit">create new</button>
  </div>
</form>
{{/if}}
<table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Name</th>
      <th scope="col">#</th>
    </tr>
  </thead>
  <tbody>
    {{#each categories}}
    <tr>
      <th scope="row">{{this.id}}</th>
      <td>{{this.name}}</td>
      <td>
        <form action="/admin/categories/{{this.id}}" style="display: inline;">
          <button type="submit" class="btn btn-link">Edit</button>
        </form>
        <form action="/admin/categories/{{this.id}}?_method=DELETE" method="POST" style="display: inline;">
          <button type="submit" class="btn btn-link">Delete</button>
        </form>
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>